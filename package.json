{
  "name": "sss-shopfront",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build:frontend": "cd web && npm install --no-audit --no-fund && node build.mjs",
    "build:public": "rm -rf public && cp -R web/dist public && rm -f public/admin.html",
    "build:db": "prisma generate && (prisma db push --skip-generate --accept-data-loss || true) && (node prisma/seed.mjs || true)",
    "build": "npm run build:db && npm run build:frontend && npm run build:public",
    "postinstall": "prisma generate || true"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/jwt": "^8.0.1",
    "@prisma/client": "^5.20.0",
    "bcryptjs": "^2.4.3",
    "fastify": "^4.28.1"
  },
  "devDependencies": {
    "prisma": "^5.20.0"
  }
}
